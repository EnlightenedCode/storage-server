general:
  artifacts:
    - target.tar.gz
test:
  override:
    - echo passing
deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - echo no-deploy-yet
      - tar czvf target.tar.gz target
  production:
    branch: master
    commands:
      - echo no-deploy-yet
      - tar czvf target.tar.gz target
