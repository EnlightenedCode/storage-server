dependencies:
  post:
    - die
general:
  artifacts:
    - target.tar.gz
deployment:
  staging:
    branch: /(feature|fix|chore).*/
    commands:
      - echo hi
      - tar czvf target.tar.gz target
  production:
    branch: master
    commands:
      - echo hi
      - tar czvf target.tar.gz target
